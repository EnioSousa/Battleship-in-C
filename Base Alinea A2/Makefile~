CC=gcc
CFLAGS=-I. -Wall
DEPS = errorMessage.h map.h point.h ship.h

ODIR=bin

_OBJ = test.o errorMessage.o map.o ship.o
OBJ = $(patsubst %,$(ODIR)/%,$(_OBJ))

$(ODIR)/%.o: %.c $(DEPS)
	$(CC) -o $@ -c $< $(CFLAGS) 

main: $(OBJ)
	$(CC) -o $@ $^ $(CFLAGS)

clean:
	rm $(ODIR)/*.o

